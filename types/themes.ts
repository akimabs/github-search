export interface Colors {
  text: string;
  warning: string;
  danger: string;
  success: string;
  background_color: string;
  secondary_color: string;
}

export enum RadiusSizes {
  small = 5,
  medium = 10,
  large = 15,
  extra_large = 20,
}
